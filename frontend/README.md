# deskflow frontend

Next.js + TypeScript + Tailwind client for the deskflow API.

```bash
npm install
cp .env.local.example .env.local   # points at the API, default http://localhost:8000
npm run dev                        # http://localhost:3000
```

Requires the Django API running (see the repo root README).
